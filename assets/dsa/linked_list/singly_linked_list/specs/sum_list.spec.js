describe('sum list', () => {
  beforeEach(() => {
    numberOfA = [6, 1, 7] // 6 is head
    numberOfB = [5, 9, 2] // 2 is head
    linkedListA = new LinkedList()
    linkedListB = new LinkedList()
    for()
    linledListA.append()
  })
  test('return desired linked list', () => {
    
  })
})
